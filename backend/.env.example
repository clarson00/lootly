# Database (PostgreSQL)
# Local development: postgres://user:password@localhost:5432/lootly
# Neon (production): postgres://user:password@ep-xxx.us-east-1.aws.neon.tech/lootly?sslmode=require
DATABASE_URL=postgres://postgres:postgres@localhost:5432/lootly

# JWT Secret (generate a random string)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Encryption Key (exactly 32 characters for AES-256)
ENCRYPTION_KEY=your-32-character-encryption-key

# Server Port (optional, defaults to 3001)
PORT=3001

# CORS Origins (comma-separated)
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# Admin App URL (for OAuth redirects)
ADMIN_APP_URL=http://localhost:5173

# Meta (Facebook/Instagram) OAuth
# Get from https://developers.facebook.com/apps/
META_APP_ID=your-meta-app-id
META_APP_SECRET=your-meta-app-secret
META_REDIRECT_URI=http://localhost:3001/api/admin/integrations/meta/callback
